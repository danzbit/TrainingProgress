# BFF (Backend for Frontend) API Configuration
# For local development (outside Docker): use http://localhost:5187 or http://localhost:5000
# For Docker environment: use http://bff:80 (internal Docker network)
# For production: use your actual server URL
VITE_API_BASE_URL=http://localhost:5187
